package model

// should not do this on real application, save the configuration on separate config file
const (
	DBHost     = "localhost"
	DBPort     = 5432
	DBUser     = "sammidev"
	DBPassword = "sammidev"
	DBName     = "todo-db"
)
